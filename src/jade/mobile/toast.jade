extends layout
block content
  
  br
  header.yy_text_center
    h2.yy_text_primary toast
  br
  br
  
  .yy_inner
    .yy_btn.yy_block.yy_success.toast--success 成功
  .yy_inner
    .yy_btn.yy_block.yy_success.toast--fail 失败
  .yy_inner
    .yy_btn.yy_block.yy_success.toast--text 纯文本
  .yy_inner
    .yy_btn.yy_block.yy_success.toast--custom 自定义
  .yy_inner
    .yy_btn.yy_block.yy_success.toast--loading 加载
  .yy_toast.yy_toast--black_trans
    .yy_mask
    .yy_toast__body
      .yy_toast__icon
        i.icon.icon-ok
      .yy_toast__text
        span 提交成功

block script
  script.
    let toast = new YYToast();
    $(document).on('click', '.toast--success', () => {
      console.log('success');
      toast.success({
        onOk: () => {
          console.log('ok');
        }
      })
    }).on('click', '.toast--fail', () => {
      console.log('fail');
      toast.cancel({
        title: '提交失败',
        duration: 1000,
      });
    }).on('click', '.toast--text', () => {
      toast.text({
        title: '文本文本文本文本文本文本',
      });
    }).on('click', '.toast--loading', () => {
      toast.loading({
        title: '正在加载中',
      });

      setTimeout(() => {
        toast.hideLoading();
      }, 2000);
    }).on('click', '.toast--custom', () => {
      toast.custom({
        title: '保存成功',
        icon: '<i class="icon icon-ok-circled">',
      });
    });
